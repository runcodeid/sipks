<p id="notice"><%= notice %></p>

<h1>B Eights</h1>

<table>
  <thead>
    <tr>
      <th>Action plan</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @b_eights.each do |b_eight| %>
      <tr>
        <td><%= b_eight.action_plan %></td>
        <td><%= link_to 'Show', b_eight %></td>
        <td><%= link_to 'Edit', edit_b_eight_path(b_eight) %></td>
        <td><%= link_to 'Destroy', b_eight, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New B Eight', new_b_eight_path %>
